export type instructorPros = {
  instructorNameLastName: {
    firstName: string;
    lastName: string;
  };
};

export type DataComing = {
    name: string;
    courseNumber: number;
    isThere: boolean;
}